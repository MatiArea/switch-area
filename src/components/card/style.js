import { StyleSheet } from 'react-native'

export const CardStyle = StyleSheet.create({
  cardComponentStyle: {
    height: '100%',
    width: '80%',
    marginHorizontal: '2%',
    marginVertical: '5%',
    justifyContent: 'center',
    alignItems: 'center',
  },
})
